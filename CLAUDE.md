# Project instructions

`AGENTS.md` is the authoritative repository workflow. In particular, every
session that changes code or project files must use its own dedicated Git
worktree and feature branch. The primary checkout is for read-only investigation
only; create the worktree before making the first file change.
